from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='a deep lneural network to detect numbers from the mnist dataset',
    author='mohamed charfi',
    license='',
)
